# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# App data — runtime DB and harness run logs (never commit)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.sqlite3
data/logs/
# Neutral persona-isolation scratch dir (created at startup; never commit)
data/persona-scratch/
# Dev mock launcher data dir + browser-verification artifacts
data-dev/
.playwright-mcp/
*.png

# Editor / OS
.DS_Store
.idea/
.vscode/

# Keep the data dir in git but not its contents
!data/.gitkeep

# Tooling caches
.codetree/
